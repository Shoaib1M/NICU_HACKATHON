/**
 * useVisualPipeline.js — NICU Guardian (Phase 3)
 * MediaPipe pose estimation + occupancy counting.
 * TODO: implement in Phase 3
 */
export function useVisualPipeline(incubatorId) {
  return { personCount: 0, nursePresent: false, motionScore: 0, isRunning: false, start: () => {}, stop: () => {} };
}
